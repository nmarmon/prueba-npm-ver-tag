//console.log("Esta es la versión 1.0.0 del proyecto")
var lodash = require ('lodash');

var output = lodash.without ([1, 2, 3], 1);
console.log (output);
console.log ("esto es un cambio un poco mas importante, es la version 1.1.0");
